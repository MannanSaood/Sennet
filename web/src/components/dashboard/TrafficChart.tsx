import { AreaChart, Area, ResponsiveContainer, XAxis, YAxis, Tooltip } from 'recharts';
export function TrafficChart({data=[]}:{data?:{time:string;rx:number;tx:number}[]}){return <div style={{height:250}}>{data.length?<ResponsiveContainer><AreaChart data={data}><XAxis dataKey="time"/><YAxis/><Tooltip/><Area dataKey="rx" stroke="#b2eb78" fill="transparent"/><Area dataKey="tx" stroke="#93b8fb" fill="transparent"/></AreaChart></ResponsiveContainer>:<p>No traffic samples in this window.</p>}</div>;}
