import { useMemo, useState } from 'react';
import { Link, useLocation } from 'react-router-dom';
import { Search } from 'lucide-react';
import { docsNavigation } from '@/content/docs/navigation';
export function DocsSidebar(){const location=useLocation();const [query,setQuery]=useState('');const sections=useMemo(()=>{const filtered=docsNavigation.filter(item=>`${item.title} ${item.description}`.toLowerCase().includes(query.toLowerCase()));return [...new Set(filtered.map(item=>item.section))].map(section=>({section,items:filtered.filter(item=>item.section===section)}))},[query]);return <aside className="docs-sidebar"><label><Search/><input type="search" value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search documentation" aria-label="Search documentation"/><kbd>/</kbd></label><nav aria-label="Documentation">{sections.map(group=><section key={group.section}><h2>{group.section}</h2>{group.items.map(item=><Link key={item.slug} to={item.slug==='introduction'?'/docs':`/docs/${item.slug}`} className={(location.pathname==='/docs'&&item.slug==='introduction')||location.pathname===`/docs/${item.slug}`?'active':''}><span>{item.title}</span><small>{item.description}</small></Link>)}</section>)}</nav><div className="docs-sidebar__status"><i/> Documentation matches the current release boundary</div></aside>}
